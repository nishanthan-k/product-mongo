spring:
    data:
        mongodb:
            username: root
            password: root
            host: localhost
            port: 27017
            database: product_db
            authentication-database: admin